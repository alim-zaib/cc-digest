Create the project file structure and `config.py` exactly as defined in SPEC.md. Create all Python files (`main.py`, `scraper.py`, `filter.py`, `ai_summary.py`, `formatter.py`, `poster.py`) as empty files with just a module docstring for now. Create `requirements.txt` with `requests>=2.31.0`. Create the `.github/workflows/digest.yml` file with the full GitHub Actions workflow as specified in SPEC.md (cron schedule, workflow_dispatch, Python setup, env vars from secrets). Verify the folder structure matches the README.
