Implement `formatter.py` following the SPEC.md specification. The `build_embeds()` function takes the filtered posts list and optional `ai_data` dict (None for now since AI is off). It should group posts by subreddit and build Discord embed payloads. Each post shows score, comment count, title, and permalink. Include the date in the header title and a footer with "Next digest at 7am UK time". Respect all Discord limits: max 10 embeds per message, max 6000 chars total, max 25 fields per embed, max 1024 chars per field value. If content exceeds limits, truncate lowest-scored posts first. If still over, return multiple payloads as a list. Also handle the empty case — if no posts made it through filtering, return a single embed saying "No Claude Code activity in the last 24 hours." Leave clear hooks for the AI-enriched format (the `ai_data` code path) but implement only the non-AI path for now.
